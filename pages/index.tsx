import Button from "@components/Button";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div className="text-black">
      <Button>Hello</Button>
    </div>
  );
};

export default Home;
